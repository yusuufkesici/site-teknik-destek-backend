meta {
  name: 03 Ticket detay (RESIDENT)
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/tickets/{{ticketId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{residentToken}}
}

assert {
  res.status: eq 200
  res.body.id: eq {{ticketId}}
  res.body.status: eq OPEN
}
