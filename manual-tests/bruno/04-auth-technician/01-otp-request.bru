meta {
  name: 01 TECHNICIAN OTP request
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/auth/otp/request
  body: json
  auth: none
}

body:json {
  {
    "phoneNumber": "{{techPhone}}"
  }
}

assert {
  res.status: eq 200
}
