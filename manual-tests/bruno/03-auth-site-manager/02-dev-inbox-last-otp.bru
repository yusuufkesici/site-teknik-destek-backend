meta {
  name: 02 Dev inbox SITE_MANAGER OTP
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/dev/sms/{{managerPhone}}/last-otp
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.code: isDefined
}

script:post-response {
  bru.setVar("managerOtp", res.body.code);
}
