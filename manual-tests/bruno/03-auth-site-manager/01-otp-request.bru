meta {
  name: 01 SITE_MANAGER OTP request
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/auth/otp/request
  body: json
  auth: none
}

body:json {
  {
    "phoneNumber": "{{managerPhone}}"
  }
}

assert {
  res.status: eq 200
}
